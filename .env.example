SECRET_KEY=change-me-super-secret
INCOMING_API_KEY=dev-incoming-token

# Demo accounts used for local testing.
DEMO_ADMIN_PASSWORD=NineCellAdmin-2026
DEMO_USER_PASSWORD=NineCellUser-2026

# RabbitMQ Management UI credentials.
# If an old RabbitMQ Docker volume already exists, recreate only the RabbitMQ volume
# or run docker compose down -v before switching credentials.
RABBITMQ_USER=ninecell
RABBITMQ_PASSWORD=NCellRabbit-2026

# Optional Telegram integration.
# Create bot via @BotFather, paste token, and set username without @.
TELEGRAM_BOT_TOKEN=
TELEGRAM_BOT_USERNAME=
TELEGRAM_LOGIN_TTL_SECONDS=600

# Public URL used in Telegram notifications and invite links.
PUBLIC_BASE_URL=http://localhost:5000
